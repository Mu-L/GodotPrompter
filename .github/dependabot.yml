version: 2
updates:
  # Workflow actions are pinned to commit SHAs; Dependabot keeps the SHA and its
  # version comment current. One grouped PR per month at most.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      github-actions:
        patterns:
          - "*"
